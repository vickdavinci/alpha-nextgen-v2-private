interface:
  display_name: "Log Analyzer"
  short_description: "Object-store-first RCA with trades.csv truth validation"
  default_prompt: "Use $log-analyzer to analyze the run with observability/object-store artifacts first, then validate realized metrics from trades.csv."
